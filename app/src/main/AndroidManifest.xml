<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="filmesPlay"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FilmesPlay"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroActivity" />

        <activity android:name=".FilmesActivity" />
        <activity android:name=".ProgramacaoActivity" />
        <activity android:name=".LojaActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".AdminProdutosActivity" />
        <activity android:name=".AdminFilmesActivity" />
        <activity android:name=".AdminProgramacaoActivity" />
        <activity android:name=".IncluirFilmeActivity" />
        <activity android:name=".EditarFilmeActivity" />
        <activity android:name=".IncluirSessaoActivity" />
        <activity android:name=".EditarSessaoActivity" />
        <activity android:name=".DetalheFilmeActivity" />
        <activity android:name=".ObrigadoActivity" />
        <activity android:name=".IncluirProdutoActivity" />
        <activity android:name=".EditarProdutoActivity" />

    </application>

</manifest>
